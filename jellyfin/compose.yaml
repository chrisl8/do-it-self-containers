services:
  jellyfin:
    container_name: jellyfin
    image: jellyfin/jellyfin
    # network_mode: 'host'
    volumes:
      - ${VOL_JELLYFIN_CONFIG:-~/container-data}/container-mounts/jellyfin/config:/config
      - ${VOL_JELLYFIN_CACHE:-~/container-data}/container-mounts/jellyfin/cache:/cache
      # You can repeat the /media subfolders to have more than one media folder
      - ${VOL_JELLYFIN_MUSIC:-~/container-data}/container-mounts/jellyfin/music:/media/music
      - ${VOL_JELLYFIN_VIDEOS:-~/container-data}/container-mounts/jellyfin/videos:/media/videos
      - ${VOL_JELLYFIN_MOVIES:-~/container-data}/container-mounts/jellyfin/movies:/media/movies
      - ${VOL_JELLYFIN_DATA:-~/container-data}/container-mounts/jellyfin:/media/downloads
      - ${VOL_JELLYFIN_AUDIOBOOKS:-~/container-data}/container-mounts/jellyfin/audiobooks:/media/audiobooks
      - ${VOL_JELLYFIN_RECON_DATA_MEDIA:-~/container-data}/container-mounts/recon/data/media:/media/recon
    ports:
      - 8096:8096
      - 8920:8920
      - 7359:7359/udp # Client discovery
      - 1900:1900/udp # DLNA
    networks:
      - jellyfin-net
    # Note that jellyfin has a health check built into the Dockerfile, so no need to add it here.
    labels:
      # For homepage
      - homepage.group=My Stuff
      - homepage.name=Jellyfin
      - homepage.weight=45
      - homepage.icon=/dashboard-icons/svg/jellyfin.svg
      - homepage.href=https://jellyfin.${TS_DOMAIN}
      - homepage.widget.type=jellyfin
      - homepage.widget.key=${API_KEY}
      - homepage.widget.url=https://jellyfin.${TS_DOMAIN}
      - homepage.widget.enableBlocks=true
      - homepage.widget.fields=["movies", "songs"]
    # GPU acceleration: if you have an NVIDIA GPU and nvidia-container-toolkit
    # installed, create a compose.override.yaml with the GPU config. See
    # docs or the gitignored compose.override.yaml on the maintainer's system.
    restart: on-failure
  ts:
    container_name: jellyfin-ts
    image: tailscale/tailscale
    environment:
      - TS_HOSTNAME=jellyfin
      - TS_EXTRA_ARGS=--advertise-tags=tag:container
      - TS_SERVE_CONFIG=/config/tailscale-config.json
      - TS_STATE_DIR=/var/lib/tailscale
      - TS_ENABLE_HEALTH_CHECK=true
      - TS_LOCAL_ADDR_PORT=127.0.0.1:9002
      - TS_AUTHKEY=${TS_AUTHKEY}
    volumes:
      - ./tailscale-state:/var/lib/tailscale
      - ./tailscale-config:/config
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - net_admin
      - sys_module
    restart: on-failure
    # https://github.com/tailscale/tailscale/issues/12758#issuecomment-2564770640
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://127.0.0.1:9002/healthz"]
      start_period: 120s
      start_interval: 5s
      interval: 5m
      timeout: 5s
      retries: 3
    networks:
      - jellyfin-net
    depends_on:
      jellyfin:
        condition: service_healthy
networks:
  jellyfin-net:
